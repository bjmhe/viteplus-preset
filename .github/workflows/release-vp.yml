name: Release Vite Plus

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    uses: bjmhe/workflows/.github/workflows/release.yml@feat/release-vp
    with:
      publish: true
    permissions:
      contents: write
      id-token: write
